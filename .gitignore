# Dependencies
node_modules/
.pnp/
.pnp.js

# Build output
dist/
dist-ssr/
build/
.vite/
*.local

# Environment & secrets — NEVER commit these (repo is PUBLIC)
.env
.env.*
!.env.example
*serviceAccount*.json
*service-account*.json
*-firebase-adminsdk-*.json
secrets/
*.pem
*.key

# Firebase
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log
.firebaserc.local

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Testing / coverage
coverage/
.nyc_output/

# Design prototypes / scratch (large self-contained bundles; keep out of the public repo)
*standalone*.html
__tmp_*
# Raw brand mockups dropped in for icon extraction (the committed source-of-record is
# pwa-icon-source.png, generated from these). Keep the bulky originals out of the repo.
Logo.jpg
Gemini_Generated_Image_*.png

# Editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
*.swp
