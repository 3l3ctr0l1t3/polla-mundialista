rules_version = '2';

// Deny-all placeholder. The real security rules are owned by ticket 003.
// Until then the database is locked down so it is never open by accident.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
